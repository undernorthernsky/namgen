# This file was generated by namgen; do not edit!

# the directory with the top-level makefile
TOP_DIR := {{TOP_DIR}}

# when calling make in *this* directory goto top-level makefile

# all-target is the same as build
all:
	make -C $(TOP_DIR) {{THIS_DIR}}_build

build:
	make -C $(TOP_DIR) {{THIS_DIR}}_build

install:
	make -C $(TOP_DIR) {{THIS_DIR}}_install

uninstall:
	make -C $(TOP_DIR) {{THIS_DIR}}_uninstall

clean:
	make -C $(TOP_DIR) {{THIS_DIR}}_clean

parent:
	make -C $(TOP_DIR)
